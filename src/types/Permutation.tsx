export type Permutation = {
    id: string,
    brand: string[],
    campaign: string,
    last_modified: Date,
    name: string,
    time_stamp: Date,
    variables: string[],
    selected?: boolean
  }